package tileGame {
	// Paintable interface
	// Anything that can be painted must have these methods
	public interface Paintable {
		// Interface methods:
		function render(xloc:int,yloc:int):void;
	}
	
}
